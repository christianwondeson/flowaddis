# Firebase Configuration
# Get these values from your Firebase Console: https://console.firebase.google.com/
# Project Settings > General > Your apps > SDK setup and configuration
#
# --- How `.env` and `.env.local` work (Next.js) ---
# Both load in development and production builds. Order: `.env` first, then `.env.local`.
# For the same variable name, `.env.local` wins.
# Keys defined only in `.env` stay available (`.env.local` does not delete them).
# Pitfall: `VAR=` or `VAR=""` in `.env.local` overrides `VAR` from `.env` with empty — avoid blank overrides unless intentional.
# Typical split: put shared secrets + production URLs in `.env`; put `BACKEND_URL=http://localhost:4000` and other dev-only overrides in `.env.local`.
# Server-only secrets (`ADMIN_FIREBASE_*`) often live only in `.env` — no need to duplicate in `.env.local` unless overriding.
#
# =============================================================================
# Fix 503 AUTH_UNAVAILABLE on POST /api/auth/password-login (Firebase → env)
# =============================================================================
# Request flow: (1) Identity Toolkit checks email/password using NEXT_PUBLIC_FIREBASE_API_KEY.
#               (2) Firebase Admin SDK mints customToken — requires ADMIN_FIREBASE_* . If Admin fails → 503.
#
# --- A) Web app (client + server routes) — Firebase Console → Project settings → General ---
# Under "Your apps" select the Web app → SDK setup (Config). Copy:
#   apiKey              → NEXT_PUBLIC_FIREBASE_API_KEY
#   authDomain          → NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
#   projectId           → NEXT_PUBLIC_FIREBASE_PROJECT_ID
#   storageBucket       → NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
#   messagingSenderId   → NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
#   appId               → NEXT_PUBLIC_FIREBASE_APP_ID
#   measurementId       → NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID (optional)
#
# --- B) Admin SDK (required to fix 503 after login succeeds server-side) ---
# Firebase Console → Project settings → Service accounts → Firebase Admin SDK → "Generate new private key".
# Open the downloaded JSON (do not commit). Copy:
#   "project_id"     → ADMIN_FIREBASE_PROJECT_ID
#   "client_email"   → ADMIN_FIREBASE_CLIENT_EMAIL
#   "private_key"    → ADMIN_FIREBASE_PRIVATE_KEY  (single env line: same PEM with \\n between lines)
#                   OR encode PEM as base64 one line → ADMIN_FIREBASE_PRIVATE_KEY_BASE64 (recommended in Firebase/GCP UIs)
#
# --- C) Named Firestore DB (if you use one) — Firestore → choose database → copy Database ID ---
#   → NEXT_PUBLIC_FIREBASE_DATABASE_ID  (must match exactly, e.g. flowaddis_db vs default)
#
# --- Where to set variables ---
# Local: `flowaddis/.env` (+ optional `flowaddis/.env.local` overrides only).
# Manual deploy (`firebase deploy --only hosting`): put A+B in `flowaddis/.env` on the computer that runs deploy; CLI loads it for the SSR function.
# App Hosting (Git): Firebase Console → Build → App Hosting → [backend] → Environment — same names; BUILD for NEXT_PUBLIC_*, RUNTIME for ADMIN_FIREBASE_*.
# If prod still 503 after CLI deploy: also check Google Cloud Console → Cloud Run → revision for SSR (`ssrflowaddisea8e1`, region europe-west1) → Variables (same ADMIN_FIREBASE_*).
# =============================================================================

# Example production project: flowaddis-ea8e1 (BookAddis / flowaddis-web)
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Server-only — download JSON: Firebase Console → Project settings → Service accounts → Generate new private key.
# JSON field mapping: project_id → ADMIN_FIREBASE_PROJECT_ID | client_email → ADMIN_FIREBASE_CLIENT_EMAIL | private_key → ADMIN_FIREBASE_PRIVATE_KEY (one quoted line, \n for newlines).
# Use ADMIN_FIREBASE_* (not FIREBASE_*) — Firebase Hosting Next.js deploy rejects env keys whose names start with FIREBASE_ (reserved prefix).
# Firebase Console → Build → App Hosting → [your backend] → Environment & secrets: add these three for RUNTIME (same values as local .env; NEXT_PUBLIC_* stays separate for the browser).
# PEM must include newlines (use \n in one line) or set ADMIN_FIREBASE_PRIVATE_KEY_BASE64 (base64 of PEM UTF-8).
#
# App Hosting checklist for POST /api/auth/password-login (503 AUTH_UNAVAILABLE = Admin SDK missing or bad PEM):
#   1. ADMIN_FIREBASE_CLIENT_EMAIL — required (service account email from the JSON; easy to forget).
#   2. ADMIN_FIREBASE_PROJECT_ID — set explicitly, or rely on GOOGLE_CLOUD_PROJECT on GCP, or NEXT_PUBLIC_FIREBASE_PROJECT_ID from build.
#   3. ADMIN_FIREBASE_PRIVATE_KEY or ADMIN_FIREBASE_PRIVATE_KEY_BASE64 — prefer BASE64 in the console (multiline PEM often gets mangled).
# Keys named FIREBASE_* work at runtime but avoid FIREBASE_* in repo .env used by `firebase deploy` (reserved); App Hosting UI accepts ADMIN_FIREBASE_*.
#
# Manual `firebase deploy --only hosting`: the SSR Cloud Function picks up env from the machine that runs deploy (reads project `.env`). Editing only Firebase Console → App Hosting env does NOT update that SSR function unless you use App Hosting–connected builds. If prod 503 but local OK: redeploy from a machine with complete ADMIN_FIREBASE_* in `.env`, or set the same variables on the Cloud Run service for `ssrflowaddisea8e1` (GCP Console).
ADMIN_FIREBASE_PROJECT_ID=
ADMIN_FIREBASE_CLIENT_EMAIL=
ADMIN_FIREBASE_PRIVATE_KEY=
# ADMIN_FIREBASE_PRIVATE_KEY_BASE64=

# NEXT_PUBLIC_APP_ORIGIN=https://bookaddis.com

# Optional: Firestore Database ID (use '(default)' for the default database)
# Example for a named database: flowaddis_db
NEXT_PUBLIC_FIREBASE_DATABASE_ID=flowaddis_db

# RapidAPI Configuration
# Get your API key from: https://rapidapi.com/
# Prefer RAPIDAPI_KEY (server-only, never exposed to the browser). Legacy name still works:
RAPIDAPI_KEY=your_rapidapi_key_here
# NEXT_PUBLIC_RAPIDAPI_KEY=your_rapidapi_key_here
#
# CI / Firebase App Hosting: add RAPIDAPI_KEY (or NEXT_PUBLIC_RAPIDAPI_KEY) in GitHub Actions secrets
# and in Firebase Console > App Hosting > Environment & secrets (BUILD + RUNTIME).

# Backend base URL
# For local development, the app defaults to http://localhost:4000 if not set
# For production, set to https://api.test.net (origin only, no /api path)
BACKEND_URL=

# Strapi CMS (Next.js /api/admin/cms/* — hotels, flights, conferences, shuttles)
# Production CMS host (HTTPS origin only, no path, no trailing slash):
STRAPI_URL=https://cms.bookaddis.com
NEXT_PUBLIC_STRAPI_URL=https://cms.bookaddis.com
# Server-only — create in Strapi Admin → Settings → API Tokens (scope for content types you edit):
STRAPI_API_TOKEN=

# reCAPTCHA Enterprise (optional — LOGIN/SIGNUP/GOOGLE_SIGNIN; public site key from GCP)
# NEXT_PUBLIC_RECAPTCHA_ENTERPRISE_SITE_KEY=6Le...
# Server verification (CreateAssessment): use the same GCP project as Firebase; enable reCAPTCHA Enterprise API.
# GOOGLE_CLOUD_PROJECT=flowaddis-ea8e1
# Application Default Credentials (local): export GOOGLE_APPLICATION_CREDENTIALS=/path/to/sa.json
# Or paste service account JSON (App Hosting / some hosts): GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}
# RECAPTCHA_MIN_SCORE=0.5
# Development: if neither GOOGLE_APPLICATION_CREDENTIALS nor GOOGLE_APPLICATION_CREDENTIALS_JSON is set, server CreateAssessment is skipped (avoids "default credentials" errors). For real local API tests, use a service account JSON.
# Local dev only — never in production: RECAPTCHA_SKIP_SERVER_VERIFY=true

# Stripe
# Publishable key used on the client
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
